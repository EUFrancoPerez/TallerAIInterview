Write comprehensive tests for the module at: $1

Include unit tests for all public functions, edge cases (null, empty,
boundary values), error cases (invalid input, network failures), and an
integration test if the module has external dependencies. Follow the
existing test patterns already used in this project.
